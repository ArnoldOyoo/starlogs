# NOTE: In production, use External Secrets Operator or AWS Secrets Manager
# This file is for reference only - do NOT commit actual secrets!
apiVersion: v1
kind: Secret
metadata:
  name: starlogs-secrets
  namespace: starlogs
type: Opaque
stringData:
  database-url: "postgresql://user:password@host:5432/starlogs_db"
  jwt-secret: "your-jwt-secret-here"
  # Add other secrets as needed
